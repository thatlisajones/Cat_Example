<h1>Emoji Diary - Basic Function</h1>

<h2>First Round of Emoji</h2>

<ul>
  {{#each feelings}}
    {{#unless selected}}
      {{> feelings/feeling-block select=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Second Round of Emoji</h2>

<ul>
  {{#each feelings}}
    {{#unless selected}}
      {{> feelings/feeling-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Third Round of Emoji</h2>

<ul>
  {{#each feelings}}
    {{#unless selected}}
      {{> feelings/feeling-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>



<h2>Rank the Selected Emoji</h2>

<ul>
  {{#each feelings}}
    {{#if selected}}
      {{> feelings/feeling-block selected=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a feeling</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">feeling Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="selected">selected?</label><br>
    <input type="radio" name="selected" value="1" checked> selected!<br>
    <input type="radio" name="selected" value="0"> Awake
  </div>

  <button type="submit">Add feeling</button>
</form>
